---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: pingers.vpn-gw.kubecombo.com
spec:
  group: vpn-gw.kubecombo.com
  names:
    kind: Pinger
    listKind: PingerList
    plural: pingers
    singular: pinger
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Pinger is the Schema for the pingers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingerSpec defines the desired state of Pinger
            properties:
              Image:
                type: string
              arpPing:
                default: "False"
                description: l2 check ip list, ip1,ip2,ip3
                type: string
              connTargets:
                description: |-
                  connection check targets:
                  protocol-ip-port
                items:
                  type: string
                type: array
              dns:
                default: "False"
                description: l7 dns check ns list, ns1,ns2,ns3
                type: string
              enableMetric:
                default: "False"
                description: enable Metrics
                type: boolean
              interval:
                default: 5
                description: defult ping, tcp, udp check interval is 5s
                type: integer
              ping:
                default: "False"
                description: l3 check ip list, ip1,ip2,ip3
                type: string
              tcpPing:
                default: "False"
                description: l4 tcp check ip:port list, ip1:port1,ip2:port2,ip3:port3
                type: string
              udpPing:
                default: "False"
                description: l4 udp check ip:port list, ip1:port1,ip2:port2,ip3:port3
                type: string
            required:
            - Image
            type: object
          status:
            description: PingerStatus defines the observed state of Pinger
            properties:
              connTargets:
                items:
                  type: string
                type: array
              enableConnCheck:
                type: boolean
              enableMetric:
                type: boolean
              externalDns:
                type: string
              image:
                type: string
              internalDns:
                type: string
              interval:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
